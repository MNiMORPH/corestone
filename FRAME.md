# Frame -- read this first

The read-first frame for this repo, per `~/.claude/COMPACTION_PLAYBOOK.md`.
Keep it current to HEAD. After a compaction, read this **before** acting, and
verify every structural claim below against git and disk before trusting it.

## (a) Origin -- why this model exists

## (b) Plan and trajectory -- stated as the next action

## (c) Key current data and objects

Files and the role each plays; branch and HEAD; the commit arc; live values and
in-flight artifacts.

## (d) Guardrails and irreversibility state

Committed vs uncommitted. Local vs pushed. Which branch. What is gated (push,
tag, release, closing issues) and therefore needs explicit authorization.

## (e) Results, each with the method that verified it

Never a bare number.

## (f) Negative results, and why not

So dead ends are not re-walked.

## (g) Reproduction

The exact command, config, and commit that regenerates each result above.
