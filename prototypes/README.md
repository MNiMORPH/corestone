Throwaway executable probes: the smallest thing that settles a design question
before the implementation is written. These are allowed to be ugly and are not
tested. Each should be referenced from the `design/*.md` whose question it
settled.

An empty directory here, while `src/` is growing, means the probe-first step is
being skipped.
